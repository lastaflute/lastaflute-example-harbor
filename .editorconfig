root = true
 
[*]
indent_style = space
charset = utf-8
# using IDE settings to keep existing files (to avoid big differences)
#end_of_line = lf

[*.{html,xml,css}]
indent_style = tab

[*.{js,json}]
indent_size = 2

[{.project,.classpath}]
indent_style = tab
